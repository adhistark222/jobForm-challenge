<?php

declare(strict_types=1);

/*
 * SubmissionMailer — placeholder only, email is intentionally deferred.
 *
 * The controller already has a stub call site (marked TODO) and the test suite has
 * a deliberately failing test for this method — both exist to make the gap visible
 * during review rather than pretend the feature is complete.
 *
 * When implemented, sendSubmissionSummary() should:
 *   - Accept the validated $payload array from the controller
 *   - Send a confirmation/summary email to jobform@voices.com
 *   - Return true on success, false on failure (so the controller can decide
 *     whether to treat a mail failure as a hard error or log and continue)
 */
class SubmissionMailer
{
    // TODO: Implement sendSubmissionSummary(array $payload): bool
}
